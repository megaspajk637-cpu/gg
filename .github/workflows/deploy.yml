name: Deploy to GitHub Pages

on:
  push:
    branches: ["main"]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create config.js with API key
        env:
          MY_API_KEY: ${{ secrets.API_KEY }}
        run: |
          echo "🔑 Создаём config.js в корне проекта..."
          echo "// Config file generated by GitHub Actions" > ./config.js
          echo "window.APP_CONFIG = {" >> ./config.js
          echo "  OPENROUTER_API_KEY: '$MY_API_KEY'" >> ./config.js
          echo "};" >> ./config.js
          echo "✅ Файл config.js успешно создан. Первые символы ключа: ${MY_API_KEY:0:10}..."

      - name: Verify files before upload
        run: |
          echo "📂 Содержимое корня проекта перед загрузкой:"
          ls -la
          echo "📄 Проверка config.js:"
          cat config.js || echo "❌ config.js не найден!"

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

      - name: DEBUG - List uploaded artifact files
        run: |
          echo "📦 Содержимое артефакта после загрузки:"
          ls -la
          echo "📄 Проверка config.js в артефакте:"
          cat config.js || echo "❌ config.js не найден!"

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4